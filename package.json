{
  "name": "vibro-webcontainer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "start:frontend": "cd frontend && npm install && npm run dev",
    "start:backend": "cd backend && npm install && npm run start:dev"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
